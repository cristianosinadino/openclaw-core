# OPS-20260323-01 — Remove Minimax configuration

## Scope / Objective
Remove the Minimax provider configuration and credentials from the OpenClaw runtime to avoid incurring extra cost.

## Intake Notes
- Requester: Cristiano via Mission Control chat (2026-03-23)
- Constraint: ensure no Minimax references remain in config; avoid exposing credentials in logs.

## Plan
1. Locate current Minimax configuration or credentials within the repo / runtime.
2. Assess dependencies; confirm removal will not break other workflows.
3. Remove configuration securely (environment variables, config files, secrets).
4. Validate no residual references remain.
5. Document validation evidence and remaining risks.

## Execution Log
1. Updated `~/.openclaw/openclaw.json` to drop the `minimax:global` auth profile, the `models.providers.minimax` block, and the `minimax/MiniMax-M2.5` fallback/alias entries; synced the sanitized file into `openclaw.json.bak` to avoid reintroducing the provider on reload.
2. Cleared cached provider metadata by overwriting `~/.openclaw/agents/main/agent/models.json` with an empty `providers` object and removing the Minimax entry from `~/.openclaw/agents/main/agent/auth-profiles.json`.
3. Restarted the OpenClaw gateway via `openclaw gateway restart` so the daemon reloads the updated configuration, then verified the service came back healthy (`openclaw gateway status`).

## Validation
- `grep -n "minimax" ~/.openclaw/openclaw.json` returns no matches (exit 1), confirming the live config is free of Minimax references.
- Spot check of `~/.openclaw/agents/main/agent/models.json` and `auth-profiles.json` confirms no Minimax provider data remains.
- Gateway status after restart shows pid 59444 listening on 127.0.0.1:18789 with RPC probe OK.

## Artifacts / Outputs
- Updated config + cache files under `~/.openclaw/`
- Service restart event recorded in LaunchAgent logs.

## Risks / Follow-ups
- Historical logs and session transcripts still mention Minimax (expected; log retention). No active configuration remains, but if future tooling replays backups outside `~/.openclaw`, we need to ensure those sources do not auto-reapply Minimax.
- Gateway continues to run from an NVM-managed Node binary; `openclaw doctor --repair` is still recommended to normalize the service environment.

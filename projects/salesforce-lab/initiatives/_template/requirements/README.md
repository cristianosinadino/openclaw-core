# Requirements Workflow

Use these folders as a lifecycle:

- `backlog/`: approved but not in execution
- `active/`: currently being implemented
- `completed/`: delivered and validated

Move requirement files through this pipeline as work progresses.
